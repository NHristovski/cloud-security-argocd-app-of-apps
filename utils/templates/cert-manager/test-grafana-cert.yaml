apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-new-tls
  namespace: monitoring
spec:
  secretName: grafana-new-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: grafana.127.0.0.1.sslip.io
  dnsNames:
    - grafana.127.0.0.1.sslip.io
